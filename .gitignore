node_modules/
.env
dist/
.svelte-kit/
*.log
.DS_Store
Thumbs.db

# apps/web/build/ NO se ignora: aqui vive el UI precompilado que se distribuye
# a este repo (no hay codigo fuente del UI en flow-connected-medica).

# Piper TTS: bin/ y models/ SI se versionan (paquete de voz vendorizado, ver
# Dockerfile) — solo se ignoran tmp/ y cache/, que son generados en runtime.
apps/api/tts/tmp/
apps/api/cache/
